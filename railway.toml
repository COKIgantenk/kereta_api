[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run prisma:generate && npm run build && npm run prisma:deploy && node dist/main.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3