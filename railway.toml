[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run prisma:generate && npm run build && ls -la dist/"

[deploy]
startCommand = "npm run prisma:deploy && node dist/main.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3